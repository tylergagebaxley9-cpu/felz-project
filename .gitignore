# Nested upstream git histories — the data is kept, the history is not.
# corpus.lock pins every upstream commit; ./fetch.sh reconstructs from source.
# nested .git dirs removed — corpus.lock pins upstream commits, fetch.sh rebuilds
**/.github/
**/node_modules/

# ── NEVER PUBLISH ─────────────────────────────────────────────────────────
# Deutsche Bibelgesellschaft NA28 / LXX. Naming a source does not create a
# licence — these are copyrighted and were pulled for personal study only.
**/GRCNA28_DBS_HS*
**/LATSEPT_DBS_HS*

__pycache__/
*.pyc
docs/walled-fetch/
tools/.gloss_index.json
